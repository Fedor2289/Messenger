# ──────────────────────────────────────────────────────────────
# requirements.txt
# Закреплены точные версии — гарантирует одинаковую работу
# локально и на Railway
# ──────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database ORM
SQLAlchemy==2.0.30

# PostgreSQL driver (нужен на Railway)
psycopg2-binary==2.9.9

# Auth: JWT токены
python-jose[cryptography]==3.3.0

# Auth: хэширование паролей
# ВАЖНО: bcrypt 4.x несовместим с passlib 1.7.4 — закрепляем версию!
passlib==1.7.4
bcrypt==4.0.1

# Validation
pydantic==2.7.1
pydantic[email]==2.7.1
email-validator==2.1.1

# Forms (нужен FastAPI для form-data)
python-multipart==0.0.9
